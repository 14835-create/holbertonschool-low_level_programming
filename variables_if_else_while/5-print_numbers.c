#include <stdio.h>

/**
 * main - prints single digit numbers base 10 from 0
 *
 * Reutnr: Always 0.
 */
int main(void)
{
	int num;

	for (num =0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
